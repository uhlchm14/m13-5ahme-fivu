{
  "name": "ue11",
  "version": "1.0.0",
  "description": "REST-Server with Node.js/express/Typescript, LA1 G1 5AHME",
  "main": "dist/main.js",
  "repository": {},
  "scripts": {
    "clean": "cd server; rm -rf dist; cd ../ngxClient; rm -rf dist",
    "build": "npm run builds, npm run buildc",
    "builds": "cd server; ./node_modules/gulp/bin/gulp.js build", 
    "buildc": "cd ngxClient; ng build --prod --output-hashing none",
    "prestart": "npm run build",
    "start": "cd server; node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "finmam13",
  "license": "ISC",
  "private": true
}
